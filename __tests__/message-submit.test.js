
describe('Message Submit', () => {
  // store create with the spy
  // Render the MessageInput with the store
  // call the action on the MessageInput
  // expect that the method emit is called on the spy with the correct arguments
});
